import {
  INCREMENT_SLIDE,
  DECREMENT_SLIDE,
} from './actions';

export const incrementSlide = () => ({
  type: INCREMENT_SLIDE,
});

export const decrementSlide = () => ({
  type: DECREMENT_SLIDE,
});
